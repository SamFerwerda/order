<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Orders - Provider View</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
            min-height: 100vh;
            padding: 20px;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 16px;
            padding: 40px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.2);
        }
        h1 {
            color: #333;
            margin-bottom: 10px;
            font-size: 2rem;
        }
        .uuid-badge {
            display: inline-block;
            background: #f0f4f8;
            padding: 8px 16px;
            border-radius: 8px;
            font-family: monospace;
            font-size: 0.9rem;
            color: #666;
            margin-bottom: 20px;
        }
        .consumer-link-section {
            background: #e8f5e9;
            border: 2px dashed #4caf50;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 30px;
        }
        .consumer-link-label {
            font-weight: 600;
            color: #2e7d32;
            margin-bottom: 10px;
            display: block;
        }
        .consumer-link-wrapper {
            display: flex;
            gap: 10px;
        }
        .consumer-link-input {
            flex: 1;
            padding: 12px 16px;
            border: 2px solid #ddd;
            border-radius: 8px;
            font-size: 0.95rem;
            background: white;
        }
        .copy-btn {
            padding: 12px 24px;
            background: #4caf50;
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            transition: background 0.2s;
        }
        .copy-btn:hover {
            background: #388e3c;
        }
        .copy-btn.copied {
            background: #2e7d32;
        }
        .add-order-section {
            margin-bottom: 30px;
        }
        .add-order-section h2 {
            color: #333;
            margin-bottom: 15px;
            font-size: 1.3rem;
        }
        .order-form {
            display: flex;
            gap: 10px;
        }
        .order-input {
            flex: 1;
            padding: 14px 18px;
            border: 2px solid #ddd;
            border-radius: 8px;
            font-size: 1rem;
            transition: border-color 0.2s;
        }
        .order-input:focus {
            outline: none;
            border-color: #11998e;
        }
        .submit-btn {
            padding: 14px 28px;
            background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            font-size: 1rem;
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(17, 153, 142, 0.4);
        }
        .orders-section h2 {
            color: #333;
            margin-bottom: 15px;
            font-size: 1.3rem;
        }
        .orders-list {
            list-style: none;
        }
        .order-item {
            background: #f8f9fa;
            border-left: 4px solid #11998e;
            padding: 20px;
            margin-bottom: 15px;
            border-radius: 0 8px 8px 0;
        }
        .order-description {
            font-size: 1.1rem;
            color: #333;
            margin-bottom: 8px;
        }
        .order-meta {
            font-size: 0.85rem;
            color: #888;
        }
        .empty-state {
            text-align: center;
            padding: 40px 20px;
            color: #888;
            background: #f8f9fa;
            border-radius: 12px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>📦 Provider Dashboard</h1>
        <div class="uuid-badge">Session: {{uuid}}</div>

        <div class="consumer-link-section">
            <label class="consumer-link-label">🔗 Share this link with your consumer:</label>
            <div class="consumer-link-wrapper">
                <input type="text" class="consumer-link-input" value="{{consumerLink}}" readonly id="consumerLink">
                <button class="copy-btn" onclick="copyLink()" id="copyBtn">📋 Copy</button>
            </div>
        </div>

        <div class="add-order-section">
            <h2>➕ Add New Order</h2>
            <form class="order-form" action="/provider/{{uuid}}/orders" method="POST">
                <input type="text" name="description" class="order-input" placeholder="Enter order description..." required>
                <button type="submit" class="submit-btn">Add Order</button>
            </form>
        </div>

        <div class="orders-section">
            <h2>📋 Current Orders ({{orders.length}})</h2>
            {{#if hasOrders}}
                <ul class="orders-list">
                    {{#each orders}}
                        <li class="order-item">
                            <div class="order-description">{{this.description}}</div>
                            <div class="order-meta">Added: {{this.createdAt}}</div>
                        </li>
                    {{/each}}
                </ul>
            {{else}}
                <div class="empty-state">
                    <p>No orders yet. Add your first order above!</p>
                </div>
            {{/if}}
        </div>
    </div>

    <script>
        function copyLink() {
            const input = document.getElementById('consumerLink');
            const btn = document.getElementById('copyBtn');
            
            input.select();
            input.setSelectionRange(0, 99999);
            
            navigator.clipboard.writeText(input.value).then(() => {
                btn.textContent = '✅ Copied!';
                btn.classList.add('copied');
                
                setTimeout(() => {
                    btn.textContent = '📋 Copy';
                    btn.classList.remove('copied');
                }, 2000);
            }).catch(() => {
                document.execCommand('copy');
                btn.textContent = '✅ Copied!';
                btn.classList.add('copied');
                
                setTimeout(() => {
                    btn.textContent = '📋 Copy';
                    btn.classList.remove('copied');
                }, 2000);
            });
        }
    </script>
</body>
</html>
